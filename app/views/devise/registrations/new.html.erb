<h4 class="before-login-heading">会員登録</h4>
<div class="container">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>

  <div class="form-group">
    <%= f.label :メールアドレス %>
    <%= f.text_field :email, class:"form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %>文字以上)</em>
    <% end %><br />
    <%= f.password_field :password, class:"form-control" %>
  </div>
  <div class="form-group">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, class:"form-control"%>
  </div>

  <div class="form-group row">
    <div class="col-3">
      <label>名前</label>
    </div>
    <div class="offset-2 col-10">
      <%= f.label :姓　 %>:<%= f.text_field :last_name, class:"form-control"%>
      <%= f.label :名　 %>:<%= f.text_field :first_name, class:"form-control"%>
    </div>
  </div>
  <div class="form-group row">
    <div class="col-3">
      <label>ナマエ</label>
    </div>
    <div class="offset-2 col-10">
      <%= f.label :セイ %> :<%= f.text_field :kana_last_name, class:"form-control" %>
      <%= f.label :メイ %> :<%= f.text_field :kana_first_name, class:"form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :表示名 %>
    <%= f.text_field :display_name, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :所属チーム名 %>
    <%= f.collection_select :team_id, Team.all, :id, :name, {:prompt => "選択してください"},{class:"form-control"} %>
  </div>
  <div class="form-group form-check">
    <%= f.check_box :agreement, :as => :boolean, checked:false %>
    <label class="form-check-label" for="user_agreement">
      利用規約とプライバシーポリシーに同意する
    </label>
  </div>
  <div class="form-group text-center">
    <%= f.submit("登録", class: "btn-primary btn-lg") %>
  </div>
<% end %>

<%= render "devise/shared/links" %>